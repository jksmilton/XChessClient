<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".CreateGameActivity" >

    <Button
        android:id="@+id/gameWithFriendButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        android:layout_centerVertical="true"
        android:onClick="requestGameWithFriend"
        android:text="@string/jksmilton.newGameWithFriend" />

    <Button
        android:id="@+id/randomGame"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_above="@+id/gameWithFriendButton"
        android:layout_alignLeft="@+id/gameWithFriendButton"
        android:onClick="requestRandomGame"
        android:text="@string/jksmilton.randomGameBttn" />

</RelativeLayout>